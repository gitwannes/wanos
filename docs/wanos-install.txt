# --- file: wanos-install.txt ---
** tools used
- Notepad++
- Everything
- Super Finder XT
- PyCharm
- SmartGit
- FileZilla
- MQTT Explorer
- SecureCRT
- Robocopy
- Chrome
- https://jsonformatter.org
- https://text-compare.com
- http://rfxcom.com


######
IEC standard 61511
EN 60335-2-53		the European safety standard for automated sauna heating appliances
######



flash Raspberry Pi with Debian 13 (Trixie) Lite (64-Bit)
	hostname wanos
	username and password: wannes / xxx
	configure wifi (country BE)
	locale timezone Europe / Brussels & keyboard layout: Generic 105 -> Belgian
	enable SSH

insert SDcard and boot
get IP and connect remotely with SSH

for info: check Debian version:
cat /etc/os-release
cat /etc/debian_version  # version number only
lsb_release -d  # distribution summary
hostnamectl  #  hostname and OS info
uname -r  # kernel version

run wanos_bootstrap_phase1.sh

** setup SMB & copy all files
sudo apt update && sudo apt install samba -y
sudo vi /etc/samba/smb.conf
sudo systemctl restart smbd
# on Windows
net use Z: "\\10.32.251.28\wanos_share" /user:wannes

run wanos_bootstrap_phase2.sh

install systemd file /etc/systemd/system/wanos.service
sudo systemctl daemon-reload
sudo systemctl enable wanos.service
sudo systemctl start wanos.service

./wanoslog.sh 1  # for consolelog





** Enable Hardware Interfaces (I²C & GPIO)
Since the LCD screens and SSR controls are wired to this Pi, you must enable the necessary hardware buses.
Open the Raspberry Pi configuration tool:
sudo raspi-config
	Navigate to Interface Options -> I2C and select Yes to enable the ARM I²C interface.
Install I²C and modern GPIO tools:
sudo apt install -y i2c-tools python3-libgpiod
to test I²C Active: Run i2cdetect -y 1. You should see the grid and the address of your connected LCD (e.g., 27 or 26).


** setup Z-Wave JS middleware
cfr zwave migration.md